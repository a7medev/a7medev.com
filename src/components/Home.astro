<style lang="scss">
  /* Heading slide animation */
  h1 > span {
    display: inline-block;
    overflow: hidden;
  }

  .sliding {
    position: relative;
    opacity: 0;
    top: -5rem;
    animation: slide 750ms ease-out forwards;
    animation-delay: var(--delay);
  }

  @keyframes slide {
    to {
      top: 0;
      opacity: 1;
    }
  }

  /* Subheading/Button fade animation */
  .subtitle, .btn {
    opacity: 0;
    animation: fade 750ms ease-out forwards;
    animation-delay: var(--delay);
  }

  @keyframes fade {
    to {
      opacity: 1;
    }
  }
</style>

<section id="home" class="container mx-auto min-h-screen p-7 lg:flex flex-row-reverse text-center lg:text-left items-center">
  <div class="mx-auto lg:ml-auto p-10 lg:w-auto">
    <picture>
      <source srcset="/images/space.webp" type="image/webp" />
      <source srcset="/images/space.png" type="image/png" />
      <img src="/images/space.png" alt="Astronaut floating in space" />
    </picture>
  </div>

  <div>
    <h1 class="font-serif text-5xl xl:text-6xl leading-[1.125] xl:leading-tight mb-9">
      <span>
        <span class="sliding" style="--delay: 0">
          Building <i class="italic text-primary">Performant</i>,
        </span>
      </span>
      <br />
      <span>
        <span class="sliding" style="--delay: 300ms">
          <i class="italic text-primary">Beautiful</i> Apps.
        </span>
      </span>
    </h1>

    <p
      class="text-xl mb-12 leading-relaxed subtitle"
      style="--delay: 800ms"
    >
      Hi! I’m Ahmed. I’m a JavaScript Developer with over 5 years of experience building quality software.
    </p>

    <a
      href="#about"
      class="shadow-lg bg-primary text-background uppercase font-bold px-7 py-4 rounded-full inline-block text-lg transition duration-300 transform hover:bg-accent hover:scale-110 hover:-rotate-2 hover:shadow-accent/25 active:scale-95 btn"
      style="--delay: 1200ms"
    >
      Get To Know Me
    </a>
  </div>
</section>
