---
import Card from './Card.svelte';

const { name, image } = Astro.props;
---

<Card lighter>
  <div class="p-5 lg:p-10">
    <img src={image} alt={name} class="aspect-square object-contain w-3/4 mx-auto mb-6" />
    <h3 class="text-lg md:text-xl font-bold">{name}</h3>
  </div>
</Card>
