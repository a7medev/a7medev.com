---
import Project from './Project.astro';

const projects = Astro.fetchContent("../projects/**/*.md");
---

<section id="work" class="bg-background-light">
  <div class="container mx-auto px-7 py-14">
    <h2 class="font-serif text-4xl mb-10">Some of <span class="italic text-primary">my work</span>.</h2>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      {projects.map(project => (
        <Project {...project} />
      ))}
    </div>
  </div>
</section>
